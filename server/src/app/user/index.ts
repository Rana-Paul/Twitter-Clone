import {types} from './types'
import { queries } from './queries'
import { resolvers } from './resolvers'
import {mutatations} from './muatation'

export const User = {types, queries, resolvers, mutatations}